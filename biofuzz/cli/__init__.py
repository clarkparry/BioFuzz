"""Command-line entry points.

`biofuzz.cli.fuzz` runs a campaign; `biofuzz.cli.triage` analyses its findings.
Both are exposed as console scripts by pyproject.toml, and as the `biofuzz-fuzz`
and `biofuzz-triage` wrappers at the repository root for use without installing.
"""
