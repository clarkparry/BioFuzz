# Runtime and test dependencies, mirroring pyproject.toml.
#
# pyproject.toml is canonical. Prefer `pip install -e ".[dev]"`, which also
# installs the biofuzz-fuzz and biofuzz-triage console scripts. This file exists
# for environments that only want the dependencies.
PyYAML>=6.0
rdkit>=2023.9.1
scipy>=1.11.0
gemmi>=0.6.0
meeko>=0.5.0
pytest>=8.0.0
