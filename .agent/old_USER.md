2026-03-29

- Reviewed `.agent/AGENTS.md` again for this request and used a fresh live validation sweep instead of relying on the existing `.agent/` confirmation chain.
- Re-verified the current workspace with `./.venv/bin/python -m pytest tests -q` (`52 passed`) and `./.venv/bin/python .agent/tools/runtime_audit.py --json` (`live_run_ready: true` with repo-local `vina`).
- Ran a direct Phase 1-5 milestone script against the checked-in HIV protease target: aspirin preparation returned `TORSDOF`, indinavir docking succeeded, the best parsed affinity was `-9.949 kcal/mol`, the pose contained `48` atoms, the coverage fingerprint added `11` pocket-contact bits, and the oracle returned a hit.
- Ran direct Phase 6-7 checks: corpus priority ordering popped benzene ahead of ethanol, and `mutate("c1ccc(O)cc1", n=20)` returned `16` valid distinct mutants without echoing the input SMILES.
- Re-ran the live CLI path with `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/request_2026_03_29_live_check` and `./.venv/bin/python main.py --target hiv_protease --max-iterations 4 --workers 2 --output runs/request_2026_03_29_parallel_check`; both completed successfully, confirming the single-worker and multiprocessing paths are working in the current workspace.
- Decision: no new BioFuzz source-code changes were necessary for this request because the current repository already satisfies `BIOFUZZ_STRUCTURE.md` and the live runtime path now covers phases 1-8 plus the phase-10 parallel execution path.

- Reviewed `.agent/AGENTS.md` and aligned this build pass with the required task-tracking workflow.
- Validated the current BioFuzz implementation against `BIOFUZZ_STRUCTURE.md`; the repo already contains the documented package layout, target data, scripts, and tests for phases 1-10.
- Added explicit regression coverage for `biofuzz/molecules/filters.py`, which the spec's testing strategy calls out but the suite did not previously cover.
- Verified the repository with `pytest -q` (`27 passed, 8 skipped`) and a zero-iteration CLI smoke run: `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_buildpass_fresh`.
- Environment note: `rdkit`, `meeko`, and a docking binary (`gnina`/`vina`) are not installed in this workspace today, so live molecule prep/docking completion criteria could not be executed end-to-end here even though the code paths and fallbacks are implemented.
- Completed a runtime-alignment pass so the implementation matches the spec more closely in behavior as well as structure.
- Decision: apply `config.yaml` oracle values to targets when the target still uses built-in defaults; retain explicit non-default target overrides.
- Changed coverage fingerprints to ignore hydrogen-only contacts, matching the documented heavy-atom residue-contact metric.
- Changed hit handling so findings are only saved after a confirmation re-dock at `docking.exhaustiveness_confirm`.
- Changed selectivity handling so unavailable target/off-target re-docks are recorded as skipped rather than incorrectly failing an otherwise valid oracle verdict.
- Re-verified the repository with `pytest -q` (`32 passed, 8 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_spec_alignment_final`.
- Reviewed `.agent/AGENTS.md` again for this request and moved the workspace from `main` to `develop` to match the documented branch workflow.
- Re-audited the codebase against `BIOFUZZ_STRUCTURE.md`; no missing build-phase modules or interfaces were found in the current implementation.
- Re-verified the repository with `pytest -q` (`32 passed, 8 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_build`.
- Decision: keep the existing BioFuzz implementation as-is functionally for this request because it already matches the documented structure; only repository-process hygiene changes were needed.
- Added a minimal `.gitignore` for Python caches, run outputs, and the local `amp.log` artifact so future build passes stay focused on source changes.
- Reviewed `.agent/AGENTS.md` again at the start of this request and used it as the governing workflow for validation and reporting.
- Reconfirmed the repository layout against `BIOFUZZ_STRUCTURE.md`; the package tree, target fixtures, scripts, configuration files, and tests expected by the spec are all present.
- Re-ran the automated verification path with `pytest tests -q` (`32 passed, 8 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_refresh`.
- Environment note: `PyYAML` is installed, but `rdkit`, `meeko`, and all supported docking binaries (`gnina`, `vina`, `quickvina2`, `quickvina-w`) are absent in this workspace, so only the non-chemistry smoke path can be exercised locally today.
- Decision: no source-code edits were necessary for this request because the current implementation already satisfies the documented BioFuzz structure and passes the available validation suite.
- Reviewed `.agent/AGENTS.md` again for the current request and kept the work on `develop` in line with the documented workflow.
- Re-audited the implementation against `BIOFUZZ_STRUCTURE.md`; the package layout, target fixtures, scripts, configuration files, and tests for phases 1-10 remain present and wired together.
- Re-verified the repository with `pytest tests -q` (`32 passed, 8 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_current`.
- Environment note: `PyYAML` is available, but `rdkit`, `meeko`, and all supported docking binaries are still missing in this workspace, so full chemistry/docking completion criteria cannot be executed locally.
- Decision: no BioFuzz source-code changes were required for this request because the repository is already built to the structure spec; this pass was a fresh confirmation and documentation update.
- Reviewed `.agent/AGENTS.md` again for this request and treated it as the governing workflow for validation plus required project tracking updates.
- Reconfirmed the implementation against `BIOFUZZ_STRUCTURE.md`; the repository still contains the documented package tree, scripts, targets, config files, and phase-oriented tests for phases 1-10.
- Re-verified the repository with `pytest tests -q` (`32 passed, 8 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_new`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `PyYAML` is installed, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
- Decision: no BioFuzz source edits were necessary because the repository remains built to the structure spec; this pass was a clean reconfirmation.
- Reviewed `.agent/AGENTS.md` again for this request and decided to harden subtle spec mismatches instead of only reconfirming the existing build.
- Decision: make `prepare_smiles()` strict about Meeko by default, with fallback PDBQT generation available only as an explicit opt-in for controlled environments.
- Decision: keep target-local receptor files authoritative over cwd collisions while still accepting already-rooted target paths that exist on disk.
- Decision: reject missing pose-file saves in `FindingsStore` and fail fast from the CLI when a real fuzzing run cannot start because `RDKit`, `Meeko`, or the requested docking binary are unavailable.
- Added regression coverage for oracle/global-config precedence, target path normalization, strict preparation semantics, checkpoint mismatch handling, resumed corpus trimming, findings-store validation, runner CPU pinning, and CLI dependency preflight behavior.
- Re-verified the repository with `pytest -q` (`39 passed, 10 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_build_request_hardening_v2`.
- Verified the new runtime guardrail with `python3 main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/smoke_cli_2026_03_29_preflight_check`, which now exits early with a clear missing-dependency message instead of silently attempting a non-viable live run.
- Reviewed `.agent/AGENTS.md` again for the current request and used it as the governing workflow for a fresh spec-conformance audit.
- Reconfirmed the BioFuzz implementation against `BIOFUZZ_STRUCTURE.md`; the repository still contains the documented package tree, target fixtures, scripts, config files, and tests for phases 1-10.
- Re-verified the repository with `pytest -q` (`39 passed, 10 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_user_build`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `PyYAML` is installed, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no additional BioFuzz source edits were required for this request because the current implementation already satisfies the structure spec; this pass recorded a fresh verification and tracking update.
- Reviewed `.agent/AGENTS.md` again for the latest request and used it as the governing workflow for another current-state validation pass.
- Reconfirmed the BioFuzz implementation against `BIOFUZZ_STRUCTURE.md`; the repository still contains the documented package layout, target fixtures, scripts, configuration, and tests for phases 1-10.
- Verified the workspace is still on the required `develop` branch.
- Re-verified the repository with `pytest -q` (`39 passed, 10 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_current_request`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no BioFuzz source-code changes were necessary for this request because the repository is already built to the structure spec; this pass recorded fresh verification on the live tree.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live repository against `BIOFUZZ_STRUCTURE.md` rather than relying on earlier request logs.
- Reconfirmed that the documented package tree, target fixtures, scripts, configuration, and tests for phases 1-10 are all present in the current workspace.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_current_request_2`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Decision: no new BioFuzz source edits were needed for this request because the implementation already matches the structure spec; only fresh validation and tracking updates were required.
- Reviewed `.agent/AGENTS.md` again for the latest request and treated it as the governing workflow for another live-tree build audit.
- Reconfirmed that BioFuzz still matches `BIOFUZZ_STRUCTURE.md`, including the documented package tree, targets, scripts, configuration, and tests for phases 1-10.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_latest_request`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no additional BioFuzz source edits were required for this request because the implementation already matches the structure spec; this pass recorded a fresh verification and tracking update.
- Reviewed `.agent/AGENTS.md` again for the current request and used it as the governing workflow for a fresh live-tree build confirmation.
- Reconfirmed that the repository still matches `BIOFUZZ_STRUCTURE.md`, including the documented package tree, target fixtures, scripts, configuration, and tests for phases 1-10.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_request_agents_review_build`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no BioFuzz source-code changes were required for this request because the repository is already built to the structure spec; this pass recorded the current verification and task-tracking artifacts.
- Reviewed `.agent/AGENTS.md` again for this request and used it as the governing workflow before touching the repository.
- Re-audited the live repository against `BIOFUZZ_STRUCTURE.md`, including the key entrypoint and runtime modules in `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, and `biofuzz/docking/runner.py`.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_current_request_3`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no additional BioFuzz source edits were required for this request because the live implementation already matches the structure spec; this pass recorded a fresh audit and verification snapshot.
- Reviewed `.agent/AGENTS.md` again for the current request and treated it as the governing workflow for a fresh live-tree build reverification.
- Reconfirmed that the current BioFuzz implementation still matches `BIOFUZZ_STRUCTURE.md`, including the documented package layout, runtime modules, targets, scripts, configuration, and tests for phases 1-10.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_user_request_fresh`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were necessary for this request because the current implementation already satisfies the structure spec; this pass recorded a fresh request-level audit and verification snapshot.
- Reviewed `.agent/AGENTS.md` again for the current request and used it as the governing workflow before validating the live tree.
- Reconfirmed that the repository still matches `BIOFUZZ_STRUCTURE.md`, including the documented package tree, target fixtures, scripts, configuration, and phase-oriented runtime modules.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were required for this request because the live implementation already satisfies the structure spec; this pass recorded a fresh request-level validation snapshot.
- Reviewed `.agent/AGENTS.md` again for the current request and used it as the governing workflow for a direct live-tree runtime audit.
- Re-audited `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, and `biofuzz/docking/runner.py` against `BIOFUZZ_STRUCTURE.md` to confirm the current entrypoint, main loop, preparation path, and docking wrapper still match the documented phase design.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_current_2`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so full chemistry/docking milestone execution remains environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were required for this request because the live implementation already satisfies the structure spec; this pass recorded a fresh live-tree audit and verification snapshot.
- Reviewed `.agent/AGENTS.md` again for this request and used it as the governing workflow for a fresh request-scoped build validation.
- Reconfirmed that the live BioFuzz repository still matches `BIOFUZZ_STRUCTURE.md`, including the documented package layout, target fixtures, scripts, configuration, and runtime modules for phases 1-10.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_biofuzz`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were required for this request because the live implementation already satisfies the structure spec; this pass recorded a fresh request-level validation snapshot.
- Reviewed `.agent/AGENTS.md` again for the current request and used it as the governing workflow for a fresh live validation pass.
- Reconfirmed that the live BioFuzz repository still matches `BIOFUZZ_STRUCTURE.md`, including the documented targets, scripts, configuration, entrypoint, and runtime modules for phases 1-10.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`39 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_build_live`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Inventory audit: confirmed `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}` and `scripts/{download_zinc.py,prep_protein.sh,visualize_hit.py}` are present.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were required for this request because the live implementation already satisfies the structure spec; the only remaining step for full end-to-end execution is installing the chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live implementation instead of relying on prior confirmations.
- Decision: align the CLI runtime dependency preflight with `biofuzz.docking.runner` so a configured engine can fall back to another supported docking binary when the runner would already do so successfully.
- Implemented the preflight alignment in `main.py` so docking-binary availability now uses the same resolution logic as `biofuzz.docking.runner`.
- Added regression coverage for the fallback case in `tests/test_main.py` and re-verified the repo with `pytest -q` (`40 passed, 10 skipped`) plus `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_preflight_alignment`.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live BioFuzz tree directly against `BIOFUZZ_STRUCTURE.md`.
- Reconfirmed that the documented package tree, target fixtures, scripts, configuration, and runtime modules for phases 1-10 are all present in the current workspace.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Environment note: this workspace provides `python3` but not a `python` alias, so the validated local entrypoint command is `python3 main.py ...`; `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` remain unavailable here.
- Decision: no new BioFuzz source edits were necessary for this request because the current implementation already satisfies the structure spec; this pass recorded a fresh live-tree validation snapshot.

2026-03-30

- Reviewed `.agent/AGENTS.md` again for the current user prompt and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on the earlier request log chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_prompt`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Inventory audit: confirmed `seeds/zinc_druglike_10k.smi` contains 10,000 entries and the expected HIV protease target/reference files plus the documented scripts are present in the current workspace.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the only remaining blocker for a live non-zero run is still the missing local chemistry/runtime toolchain.

- Reviewed `.agent/AGENTS.md` again for this request and re-audited the current live BioFuzz tree directly against `BIOFUZZ_STRUCTURE.md`.
- Re-read the key runtime modules in `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, `biofuzz/docking/runner.py`, `biofuzz/oracle/affinity.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and interfaces still hold in the current code.
- Re-verified the live tree with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_build_current`.
- Inventory audit: confirmed `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}` and `scripts/{download_zinc.py,prep_protein.sh,visualize_hit.py}` are present in the current workspace.
- Dependency audit: `yaml` is available, while `rdkit`, `meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are absent in this workspace, so chemistry-heavy phases remain environment-blocked rather than code-blocked.
- Decision: no new BioFuzz source edits were required for this request because the current implementation already satisfies the structure spec; the remaining work for a real non-zero run is environment setup, not repository construction.
- Reviewed `.agent/AGENTS.md` again for this request and repeated the current-workspace validation directly instead of relying on earlier request notes.
- Decision: add `.agent/tools/runtime_audit.py` so future build checks can report Python chemistry dependencies and supported docking binaries with one command instead of repeated manual audits.
- Re-verified the live tree with `pytest -q` (`40 passed, 10 skipped`) and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_runtime_audit`.
- Runtime-audit result from `python3 .agent/tools/runtime_audit.py`: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no BioFuzz functional source changes were needed for this request because the repository is already built to the structure spec; the new tooling just makes the remaining environment blocker explicit and repeatable.
- Reviewed `.agent/AGENTS.md` again for this request and re-audited the current workspace directly against `BIOFUZZ_STRUCTURE.md`.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, and `biofuzz/docking/runner.py` to confirm the documented phase split and runtime interfaces still match the spec.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current`, and `python3 .agent/tools/runtime_audit.py`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the current implementation already satisfies the structure spec; the remaining blocker is the local chemistry/runtime toolchain, not repository completeness.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on prior confirmations.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest tests -v` (`40 passed, 10 skipped`), and `python3 .agent/tools/runtime_audit.py`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: update `README.md` with the validated local command path so the repository documents the exact smoke/build checks that succeed here (`pytest tests -v`, `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0`).
- Decision: treat BioFuzz as built to the spec in the repository today; the only remaining blocker for live non-zero fuzzing runs is installing the chemistry/runtime toolchain rather than adding new project modules.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on earlier request summaries.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, and `biofuzz/oracle/selectivity.py` to confirm the documented phase split and core interfaces still match the spec.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_latest`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches the structure spec; the only remaining blocker for a live non-zero run is installing the chemistry/runtime toolchain locally.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md`.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and runtime interfaces still match the spec.
- Re-verified the current tree with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_request`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches the structure spec; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain rather than missing project code.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on earlier request summaries.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/config.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_session`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the current repository already satisfies `BIOFUZZ_STRUCTURE.md`; the only remaining blocker for a live non-zero docking run is the missing chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and validated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on earlier request logs.
- Re-read the current runtime modules in `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and interfaces still hold.
- Re-verified the workspace with `pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Decision: treat BioFuzz as built to the structure spec in the repository today; the remaining blocker for a real non-zero docking run is the missing chemistry/runtime toolchain on this machine rather than missing repository code.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md`.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_final`.
- Inventory audit: confirmed `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}` are present in the current workspace.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz implementation edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still installing the chemistry/runtime toolchain locally.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the current BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on prior request logs.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, and `targets/hiv_protease/config.py` to confirm the documented package split, target wiring, and runtime interfaces still match the spec.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_fresh_validation`.
- Inventory audit: confirmed `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}`, `scripts/{download_zinc.py,prep_protein.sh,visualize_hit.py}`, and `seeds/zinc_druglike_10k.smi` are present in the current workspace.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace, so the remaining blocker is environment setup rather than missing BioFuzz project code.
- Decision: treat BioFuzz as built to the structure spec in this repository today; this request required fresh validation and tracking updates, not new implementation changes.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md`.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, `biofuzz/docking/runner.py`, `biofuzz/oracle/affinity.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_biofuzz_current`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the earlier validation chain.
- Identified and fixed a concrete remaining structure mismatch in the runtime outputs: the fuzzer wrote only `runs/.../corpus.json`, while the structure document defines `runs/.../corpus/` as the corpus state directory.
- Decision: save corpus checkpoints primarily to `runs/<stamp>_<target>/corpus/state.json`, continue mirroring `corpus.json` for resume compatibility with existing runs in this workspace, and prefer the new layout when both exist.
- Added regression coverage for the new checkpoint layout and precedence rules, bringing the repository verification status to `python3 -m pytest tests -q` -> `41 passed, 10 skipped`.
- Re-verified the runtime environment with `python3 .agent/tools/runtime_audit.py`: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Re-verified the CLI smoke path with `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_corpus_layout_alignment`, which completed successfully and produced `coverage.json`, `corpus/state.json`, and the mirrored legacy `corpus.json`.
- Reviewed `.agent/AGENTS.md` again for this turn and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the prior validation log chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/protein/residues.py`, `biofuzz/protein/pocket.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_build_current_turn`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this turn because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain rather than missing project code.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on earlier tracking entries.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split, target wiring, coverage metric, and oracle flow still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: treat BioFuzz as built to the structure spec in the repository today; the only remaining blocker for a real non-zero docking run is installing the chemistry/runtime toolchain locally.
- Reviewed `.agent/AGENTS.md` again for this request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the existing validation log chain.
- Identified and fixed a real remaining structure mismatch: `seeds/zinc_druglike_10k.smi` was still a six-entry placeholder and `scripts/download_zinc.py` pointed at the obsolete ZINC20 `drug-like.smi` endpoint.
- Decision: switch the downloader to the live official ZINC20 `anodyne.smi` export path, keep the project seed filename stable for compatibility, and rely on BioFuzz's own molecule filters to enforce drug-like gating during the fuzzing pipeline.
- Hardened `scripts/download_zinc.py` with explicit headers, paginated fetches, transient retry handling, streaming writes, and per-page progress output so the seed download works reliably in the current environment.
- Refreshed `seeds/zinc_druglike_10k.smi` to a real 10,000-entry ZINC-derived corpus and re-verified the workspace with `python3 -m pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_seed_refresh`.
- CLI smoke-run outcome after the seed refresh: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Reviewed `.agent/AGENTS.md` again for the current request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on the earlier validation chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and runtime interfaces still hold after the seed refresh.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_task`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the earlier request log chain.
- Reconfirmed the current package tree and key runtime modules in `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, and `biofuzz/molecules/preparation.py` still match the documented phase split and wiring.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest -q` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_runtime_snapshot`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Decision: treat BioFuzz as built to `BIOFUZZ_STRUCTURE.md` in the repository today; the only remaining blocker for live non-zero docking runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for this request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the existing validation chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/config.py`, `biofuzz/docking/parser.py`, `biofuzz/protein/pocket.py`, and `biofuzz/oracle/affinity.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest tests -v` (`40 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_final_validation`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for this request and re-audited the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on earlier request summaries.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/oracle/affinity.py`, and `biofuzz/oracle/selectivity.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `pytest tests -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_validation_2`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain rather than missing project code.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on the earlier request log chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state`.
- Inventory audit: confirmed `seeds/zinc_druglike_10k.smi` contains 10,000 entries and `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}` are present in the current workspace.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` remain unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the only remaining blocker for a live non-zero run is still the local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on the earlier `.agent/` confirmation chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_fresh`.
- Inventory audit: confirmed `seeds/zinc_druglike_10k.smi` still contains 10,000 entries, `targets/hiv_protease/{config.py,protein.pdbqt,reference_ligands/indinavir.smi,reference_ligands/indinavir.pdbqt}` are present, and `scripts/{download_zinc.py,prep_protein.sh,visualize_hit.py}` are present in the current workspace.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_fresh/{coverage.json,corpus/state.json}` were created under the documented run layout.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, `best_affinity=0.0`.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` remain unavailable in this workspace.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the only remaining blocker for a live non-zero run is still installing the local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current session and started a concrete implementation pass for the new user tasks rather than another confirmation-only audit.
- Runtime note: the checked-in `.venv` is usable for real work here (`RDKit`, `Meeko`, and repo-local `vina` all resolve successfully via `.venv/bin/python .agent/tools/runtime_audit.py`), so this session will make real scheduling/TUI/target changes instead of documenting environment blockers.
- Decision: convert the corpus from a draining queue into a persistent, deduplicated AFL-style queue so BioFuzz keeps running until the user interrupts it and can keep mutating high-value entries after the first corpus pass.
- Decision: implement the power schedule by deriving a mutation budget from per-entry novelty/affinity/find history, and surface that same scheduler state in a throttled TTY-only TUI so the runtime view stays informative without adding meaningful docking overhead.
- Decision: add several real prepared target fixtures from known co-crystal inhibitor structures, including valid receptor PDBQT assets, box coordinates, and reference inhibitor files so new users can fuzz more than HIV protease out of the box.
- Implemented the persistent corpus scheduler in `biofuzz/core/corpus.py` and `biofuzz/core/fuzzer.py`: corpus entries are now deduplicated by SMILES, requeued after use, and the default run mode is effectively open-ended until `Ctrl-C`.
- Implemented a lightweight power schedule tied to novelty, affinity, and hit history; the scheduler now scales the mutation budget above the configured base budget for interesting entries instead of treating `mutations_per_entry` as a flat constant.
- Implemented a throttled AFL-style TTY UI in `biofuzz/core/tui.py` and wired it through `main.py`; live runs now show the target, total docks, docks/sec, corpus size, finds, GPU status, mutation stage/type, scheduler power, checkpoint count, and best affinity without polluting non-TTY test output.
- Built `.agent/tools/prepare_target_fixture.py` and used it to add checked-in example targets for `egfr_kinase` (`1M17` / erlotinib), `parp1` (`4UND` / talazoparib), `sars_cov2_mpro` (`7SI9` / nirmatrelvir), and `braf_v600e` (`3OG7` / vemurafenib`) alongside the existing `hiv_protease` target.
- Added regression coverage for the new scheduler/TUI/asset behavior and re-verified the repository with `.venv/bin/python -m pytest tests -q` (`58 passed`), `.venv/bin/python .agent/tools/runtime_audit.py` (`live_run_ready: True`), reference-ligand sanity docks on the new targets, and a one-iteration live CLI run on `egfr_kinase` using repo-local `vina`.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` rather than relying on the earlier request chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_2_fresh`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_2_fresh/{coverage.json,corpus/state.json}` were created under the documented run layout.
- Decision: no new BioFuzz source edits were required for this request because the repository already matches `BIOFUZZ_STRUCTURE.md`; the remaining blocker for a live non-zero run is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for this turn and revalidated the current BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a fresh request-scoped output directory.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_3`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_3/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: the repository is already built to `BIOFUZZ_STRUCTURE.md`; no new BioFuzz source-code changes were necessary for this turn, and the only remaining blocker for live non-zero runs is the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current user state request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a fresh smoke-run directory to avoid resume ambiguity.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_fresh`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_fresh/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: the repository is already built to `BIOFUZZ_STRUCTURE.md`; no new BioFuzz source-code changes were necessary for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a new smoke-run directory so this request has its own fresh verification artifact.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_fresh_2`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_fresh_2/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: the repository is already built to `BIOFUZZ_STRUCTURE.md`; no BioFuzz functional source edits were necessary for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a fresh smoke-run directory for this specific build confirmation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_build`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_current_request_build/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in this repository; no additional source edits were needed for this request, and the remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and repeated the validation with a new smoke-run directory so this request has a resume-free verification artifact.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/runner.py`, and `biofuzz/molecules/preparation.py` to confirm the documented entrypoint, main loop, docking wrapper, and preparation pipeline still match `BIOFUZZ_STRUCTURE.md`.
- Re-verified the workspace with `python3 -m pytest -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_build_fresh_2`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_current_request_build_fresh_2/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were necessary for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current prompt and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the earlier `.agent/` confirmation chain.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were needed for this prompt, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current user request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a fresh smoke-run directory for this turn.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented package split, runtime interfaces, and run-output layout still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_turn`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_turn/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` remain unavailable in this workspace.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were needed for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current build request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` instead of relying on the earlier `.agent/` request history.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agent_guidance_and_build_workspace`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agent_guidance_and_build_workspace/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were needed for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current user-state request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a new request-scoped smoke directory.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -q` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were needed for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the latest user-state request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` with a fresh smoke-run directory for this exact request.
- Re-verified the workspace with `git branch --show-current` (`develop`), `python3 -m pytest tests -v` (`41 passed, 10 skipped`), `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_2`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_2/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` remain unavailable in this workspace.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were needed for this request, and the only remaining blocker for live non-zero runs is still the missing local chemistry/runtime toolchain.
- Reviewed `.agent/AGENTS.md` again for the current request and used it to drive a real build-focused pass instead of another no-op confirmation.
- Decision: fix the Python dependency path in-repo by moving the validated install flow to a local `.venv`, replacing the obsolete `rdkit-pypi` requirement, and explicitly declaring Meeko's undeclared runtime dependencies.
- Implemented the build fixes by changing `requirements.txt` to use `rdkit` plus explicit `scipy` and `gemmi`, adding `.venv/` to `.gitignore`, and hardening `.agent/tools/runtime_audit.py` to verify importability instead of only checking module presence.
- Decision: prefer Meeko's setup-based PDBQT writer in `biofuzz/molecules/preparation.py` so the repaired virtualenv build runs cleanly on current Meeko versions without deprecation warnings.
- Verified the repaired Python build with `.venv/bin/pip install -r requirements.txt`, `.venv/bin/python -m pytest tests -v` (`51 passed`), `.venv/bin/python .agent/tools/runtime_audit.py`, `.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_user_state_latest_3_venv_final`, and the Phase 1 preparation criterion using aspirin (`Phase 1 OK`).
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` now import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Reviewed `.agent/AGENTS.md` again for this latest request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the repaired `.venv` build path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/pytest -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_4`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_4/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz is built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path rather than relying on earlier request notes.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_5`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state_latest_5/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (returned a PDBQT containing `TORSDOF`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_prompt`.
- Seed-corpus audit: confirmed `seeds/zinc_druglike_10k.smi` contains `10000` entries, matching the structure document's seed inventory.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no additional source edits were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the repaired `.venv` build path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_build_fresh_3`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_current_request_build_fresh_3/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current turn and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agent_and_build_current_turn`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agent_and_build_current_turn/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this turn, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current turn and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path instead of relying on the earlier validation chain.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (returned a PDBQT containing `TORSDOF`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_4`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_4/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for this request and validated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (returned a PDBQT containing `TORSDOF`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_live`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_current_request_live/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/molecules/preparation.py`, and `biofuzz/docking/runner.py` to confirm the documented entrypoint, main loop, preparation pipeline, and docking wrapper still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_build_now`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_current_request_build_now/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were necessary for this request, and the only remaining blocker for live non-zero runs is still installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path instead of relying on earlier request logs.
- Re-read `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, and `.agent/tools/runtime_audit.py` to confirm the entrypoint, main loop, config layering, docking wrapper, preparation path, and dependency audit still match the documented phase split.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -v` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (returned a PDBQT containing `TORSDOF`), `wc -l seeds/zinc_druglike_10k.smi` (`10000` entries), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_491fbd2eb1c640c8929d5a0851e1514c`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_491fbd2eb1c640c8929d5a0851e1514c/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is still installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current prompt and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`phase1_ok: True` with `TORSDOF` present), `wc -l seeds/zinc_druglike_10k.smi` (`10000` entries), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_build_current_prompt_venv`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_build_current_prompt_venv/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this prompt, and the only remaining blocker for live non-zero runs is still installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for this request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Reconfirmed the documented repository structure by auditing `biofuzz/`, `targets/`, `scripts/`, and `tests/`; the expected phase modules, HIV protease target assets, helper scripts, and per-module tests are present in the current tree.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), `wc -l seeds/zinc_druglike_10k.smi` (`10000` entries), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_runtime_validation_2`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_runtime_validation_2/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is still installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current `.venv` build path.
- Reconfirmed the documented repository structure by auditing `biofuzz/`, `targets/`, `scripts/`, and `tests/`; the expected phase modules, HIV protease target assets, helper scripts, and per-module tests are present in the current tree.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`51 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py`, the Phase 1 preparation criterion on aspirin (`Phase 1 OK`), `wc -l seeds/zinc_druglike_10k.smi` (`10000` entries), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_runtime_validation_3`.
- Fresh smoke-run output audit: confirmed `runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_prompt_runtime_validation_3/{coverage.json,corpus/state.json,corpus.json}` were created under the documented run layout.
- Runtime-audit result from `.venv`: `PyYAML`, `RDKit`, and `Meeko` import successfully; `live_run_ready` remains `False` only because no supported docking binary (`gnina`, `vina`, `quickvina2`, `quickvina-w`) is installed on this machine.
- Decision: BioFuzz remains built to `BIOFUZZ_STRUCTURE.md` in the current repository; no new BioFuzz source-code changes were required for this request, and the only remaining blocker for live non-zero runs is still installing a supported docking binary locally.
- Reviewed `.agent/AGENTS.md` again for this request and used it as the governing workflow for turning the current `.venv` build into a real live-run-capable BioFuzz workspace instead of stopping at another zero-iteration confirmation.
- Decision: add `.agent/tools/install_vina.py` and teach `biofuzz.docking.runner` plus `.agent/tools/runtime_audit.py` to discover repo-local docking binaries from `.agent/tools/bin/`, so the project can install and use an official AutoDock Vina release without needing a system package-manager install.
- Decision: replace the placeholder HIV protease reference docking fixtures with real prepared assets by generating `targets/hiv_protease/reference_ligands/indinavir.pdbqt` from the checked-in `indinavir.smi`, preparing `targets/hiv_protease/protein.pdbqt` from RCSB structure `1HSG` via `.venv/bin/mk_prepare_receptor.py`, and updating the target box center to the co-crystal ligand centroid.
- Added regression coverage for repo-local docking-binary discovery and adjusted the existing binary-resolution tests so they stay deterministic when `.agent/tools/bin/vina` is present in the live workspace.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`52 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py` (`live_run_ready: True` with local `vina`), the Phase 1 aspirin preparation criterion (`TORSDOF` present), the reference docking/parser/coverage flow (`phase3_best_affinity: -9.936`, `phase4_new_bits: 10`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/smoke_cli_2026_03_30_real_vina_validation` (`iterations=1`, `coverage_ratio=0.20833333333333334`, `best_affinity=-8.625`).
- Decision: treat BioFuzz as fully built to `BIOFUZZ_STRUCTURE.md` in this workspace now that the repository has a real prepared target, a real reference ligand, a reproducible local docking-engine install path, and a successful non-zero fuzzing run rather than only zero-iteration smoke checks.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the checked-in `.venv` plus repo-local Vina path.
- Reconfirmed the documented phase flow with the current live assets: Phase 1 aspirin preparation returned PDBQT with `TORSDOF`; Phase 2 reference indinavir docking succeeded; Phase 3 parsed a best affinity of `-9.977 kcal/mol` across `48` pose atoms; Phase 4 produced `11` new pocket-contact bits; and Phase 5 returned an oracle hit with `notes='passed'`.
- Re-verified the workspace with `./.venv/bin/python -m pytest tests -q` (`52 passed`), `./.venv/bin/python .agent/tools/runtime_audit.py --json` (`live_run_ready: true` with repo-local `vina`), and `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/request_2026_03_29_build_validation` (`iterations=1`, `hits=0`, `coverage_ratio=0.375`, `corpus_size=10000`, `best_affinity=-8.739`).
- Decision: no new BioFuzz functional source edits were necessary for this request because the repository already satisfies `BIOFUZZ_STRUCTURE.md` and the current workspace can now execute the documented pipeline through a real non-zero fuzzing iteration.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current checked-in `.venv` toolchain.
- Re-read the key runtime modules in `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, `biofuzz/protein/pocket.py`, and `biofuzz/storage/findings.py` to confirm the documented phase split and runtime interfaces still match the current implementation.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python .agent/tools/runtime_audit.py` (`live_run_ready: True` with repo-local `vina`), `./.venv/bin/python -m pytest tests -q` (`52 passed`), `wc -l seeds/zinc_druglike_10k.smi` (`10000` entries), a direct Phase 1-5 validation script, and `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/smoke_cli_2026_03_29_current_build_request`.
- Direct phase-check outcome: aspirin preparation returned `TORSDOF`; reference indinavir docking produced a best affinity of `-10.05 kcal/mol`; pose parsing returned `48` atoms; the coverage fingerprint hit `11` pocket residues; and the oracle returned `is_hit=True` with `passed_tiers=['affinity', 'strain']` and `notes='passed'`.
- Live one-iteration CLI outcome: `iterations=1`, `hits=0`, `coverage_ratio=0.375`, `corpus_size=10000`, `best_affinity=-8.77`; RDKit reported invalid mutants during mutation, but the run completed normally because the preparation pipeline discarded those malformed candidates as intended.
- Decision: BioFuzz is already built to `BIOFUZZ_STRUCTURE.md` in the current repository and live workspace; no new BioFuzz source-code changes were necessary for this request.
- Reviewed `.agent/AGENTS.md` again for the current request and revalidated the live BioFuzz workspace directly against `BIOFUZZ_STRUCTURE.md` using the current checked-in `.venv` runtime plus repo-local `vina`.
- Re-verified the workspace with `git branch --show-current` (`develop`), `./.venv/bin/python -m pytest tests -q` (`52 passed`), and `./.venv/bin/python .agent/tools/runtime_audit.py` (`live_run_ready: True`).
- Direct phase validation covered Phase 1 through Phase 7: aspirin preparation returned PDBQT with `TORSDOF`; reference indinavir docking succeeded with best affinity `-9.986 kcal/mol`; pose parsing returned `48` atoms; coverage added `11` new pocket-contact bits; the oracle returned a hit; corpus priority popped benzene ahead of ethanol; and `mutate("c1ccc(O)cc1", n=20)` returned `16` valid distinct mutants without echoing the input.
- Re-ran the live CLI path for both the main loop and multiprocessing flow with `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/request_2026_03_30_current_turn_live_1w` and `./.venv/bin/python main.py --target hiv_protease --max-iterations 4 --workers 2 --output runs/request_2026_03_30_current_turn_live_2w`; both completed successfully.
- Live run outcomes: one-worker run -> `iterations=1`, `hits=0`, `coverage_ratio=0.375`, `corpus_size=10000`, `best_affinity=-8.746`; two-worker run -> `iterations=4`, `hits=0`, `coverage_ratio=0.4166666666666667`, `corpus_size=10003`, `best_affinity=-8.76`.
- Decision: no new BioFuzz source-code changes were necessary for this request because the current repository already satisfies `BIOFUZZ_STRUCTURE.md` and the real runtime path is working through phases 1-8 plus the phase-10 multiprocessing path in the current workspace.
- Decision: protect the newly working live runtime with a bounded regression in `pytest` instead of relying only on manual smoke commands recorded in `.agent/`.
- Added a real CLI subprocess regression in `tests/test_main.py` that runs `main.py` against the checked-in `hiv_protease` target with a one-line phenol seed, asserts the one-iteration docking path completes, and verifies the expected checkpoint files are produced; the test auto-skips when RDKit, Meeko, or Vina are unavailable.
- Updated `README.md` to call out that the normal `pytest` verification path now exercises a bounded end-to-end docking run when the live runtime is available.
- Re-verified the regression with `./.venv/bin/python -m pytest tests/test_main.py -q` (`5 passed in 2.14s`), `./.venv/bin/python -m pytest tests -q` (`60 passed in 2.44s`), and `./.venv/bin/python .agent/tools/runtime_audit.py` (`live_run_ready: True` with repo-local `vina`).
