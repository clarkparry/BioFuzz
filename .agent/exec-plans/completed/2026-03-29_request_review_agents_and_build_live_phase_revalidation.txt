Task: Review `.agent/AGENTS.md` and validate/build the current BioFuzz workspace against `BIOFUZZ_STRUCTURE.md`, including the later-phase corpus, mutation, and parallel-run behavior.

Challenges:
- Prove the current repository state from the live workspace instead of relying on the long existing confirmation history under `.agent/`.
- Cover more than the early preparation/docking milestones by also checking corpus prioritization, mutation output quality, and multiprocessing execution.
- Avoid unnecessary source churn if the repository already satisfies the structure document and the live runtime path is healthy.

Approaches Considered:
- Rebuild or refactor BioFuzz modules preemptively from the structure document even if the current implementation already matched it.
- Run a fresh request-scoped validation sweep across tests, runtime readiness, direct phase checks, and live CLI runs, then only change code if a concrete mismatch appears.

Successful Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`.
- Ran `./.venv/bin/python -m pytest tests -q`.
- Ran `./.venv/bin/python .agent/tools/runtime_audit.py --json` and confirmed repo-local `vina` resolution.
- Confirmed the seed corpus still contains 10,000 entries with `wc -l seeds/zinc_druglike_10k.smi`.
- Executed a direct Phase 1-5 validation script covering aspirin preparation, indinavir docking, pose parsing, coverage fingerprinting, and oracle evaluation against the checked-in HIV protease target.
- Executed a direct Phase 6-7 validation script covering corpus priority ordering and valid distinct mutation output for phenol.
- Executed `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/request_2026_03_29_live_check`.
- Executed `./.venv/bin/python main.py --target hiv_protease --max-iterations 4 --workers 2 --output runs/request_2026_03_29_parallel_check`.
- Recorded the request outcome in `.agent/USER.md`, `.agent/PLANS.md`, and `.agent/PHASES.md`.

Outcome:
- Verification status: `52 passed`.
- Runtime status: `live_run_ready: True`; `vina` resolved from `.agent/tools/bin/vina`.
- Direct Phase 1-5 results: `phase1_ok=True`; `phase2_ok=True`; `phase3_best_affinity=-9.949`; `phase3_atoms=48`; `phase4_new_bits=11`; `phase5_hit=True`.
- Direct Phase 6-7 results: `phase6_top=c1ccccc1`; `phase7_count=16`; all returned mutants parsed successfully; the input phenol SMILES was not returned.
- Live CLI results: one-worker run completed with `iterations=1`, `hits=0`, `coverage_ratio=0.3333333333333333`, `corpus_size=10000`, `best_affinity=-8.638`; two-worker run completed with `iterations=4`, `hits=0`, `coverage_ratio=0.4583333333333333`, `corpus_size=10003`, `best_affinity=-8.938`.
- Implementation status: the current repository already satisfies `BIOFUZZ_STRUCTURE.md`; no new BioFuzz source-code changes were necessary for this request.
