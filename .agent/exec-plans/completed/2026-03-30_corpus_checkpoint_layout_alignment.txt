Task: Review `.agent/AGENTS.md`, validate the current BioFuzz build against `BIOFUZZ_STRUCTURE.md`, and fix any concrete repository-layout mismatch that still exists in the runtime output structure.

Challenges:
- Keep the fix small and local to the real mismatch instead of reworking already-correct pipeline code.
- Align new run artifacts with the structure document without breaking resume behavior for existing `runs/*/corpus.json` checkpoints already present in the workspace.
- Prove the change with targeted regression coverage plus the existing repo-level validation path.

Approach:
- Re-read `.agent/AGENTS.md`, `BIOFUZZ_STRUCTURE.md`, and the current runtime modules to find a concrete implementation/spec mismatch.
- Patch the runtime to emit the documented `runs/.../corpus/` layout while preserving compatibility with legacy checkpoints.
- Add or update regression coverage for the new layout and resume precedence.
- Re-run the available verification path (`pytest`, runtime audit, and zero-iteration CLI smoke run).

Completed Approach:
- Re-audited the live implementation and identified that the fuzzer still checkpointed the evolving queue only as `runs/.../corpus.json`, while `BIOFUZZ_STRUCTURE.md` defines `runs/.../corpus/` as a directory.
- Updated `biofuzz/core/fuzzer.py` to save corpus state primarily to `runs/.../corpus/state.json`, load from that path when present, fall back to legacy `corpus.json` checkpoints for resume compatibility, and mirror saves back to the legacy file so older runs remain usable.
- Added regression coverage in `tests/test_fuzzer.py` to verify the new checkpoint path is created, the legacy mirror is retained, and the new layout takes precedence when both checkpoint forms exist.
- Updated `README.md` to document the current checkpoint layout and re-ran the repository verification path.

Outcome:
- Source change: run outputs now match the documented `runs/.../corpus/` directory layout while preserving compatibility with existing `corpus.json` checkpoints.
- Verification status: `python3 -m pytest tests -q` -> `41 passed, 10 skipped`.
- Smoke status: `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_corpus_layout_alignment` completed successfully and produced `corpus/state.json`, `corpus.json`, and `coverage.json`.
- Remaining blocker: this workspace still lacks `RDKit`, `Meeko`, and all supported docking binaries, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
