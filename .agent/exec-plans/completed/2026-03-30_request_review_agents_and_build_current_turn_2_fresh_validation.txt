Task: Review `.agent/AGENTS.md` and validate/build the current BioFuzz workspace against `BIOFUZZ_STRUCTURE.md` with a fresh request-scoped smoke run.

Challenges:
- Validate the live repository directly instead of relying on the long chain of prior `.agent/` confirmations.
- Keep the request closure accurate by distinguishing repository completeness from the missing local chemistry/runtime toolchain.
- Record this request cleanly in the required `.agent` tracking files without making speculative BioFuzz source edits.

Approaches Considered:
- Rebuild modules from the structure document again even though the repository already contains the documented package tree and phase modules.
- Re-audit the live implementation, rerun the current verification path, and only change source files if a concrete mismatch appears.

Successful Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`.
- Re-audited `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` against the documented phase design.
- Re-verified the workspace with `git branch --show-current`, `python3 -m pytest -q`, `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_turn_2_fresh`.
- Confirmed the fresh smoke-run output created `coverage.json` plus `corpus/state.json` under the documented run layout.

Outcome:
- Branch status: `develop`.
- Verification status: `41 passed, 10 skipped` and a successful fresh zero-iteration CLI smoke run with `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, and `best_affinity=0.0`.
- Implementation status: the current repository already matches `BIOFUZZ_STRUCTURE.md`, so no new BioFuzz source-code edits were required for this request.
- Runtime status: `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable in this workspace.
- Remaining blocker: live non-zero chemistry/docking runs remain environment-blocked until the local chemistry/runtime toolchain is installed.
