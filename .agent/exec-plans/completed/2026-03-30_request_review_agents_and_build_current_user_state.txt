Task: Review `.agent/AGENTS.md` and validate/build the current BioFuzz workspace against `BIOFUZZ_STRUCTURE.md` for the current user-state request.

Challenges:
- Validate the live repository directly instead of relying on the many earlier request summaries already recorded under `.agent/`.
- Confirm the core runtime interfaces still match the documented phase split across preparation, docking, parsing, coverage, oracle, mutation, corpus, and entrypoint layers.
- Distinguish repository completeness from environment/toolchain limitations so the request closes accurately without inventing unnecessary code changes.

Approaches Considered:
- Rebuild or refactor BioFuzz modules again from the structure document.
- Perform a live code/spec audit of the existing implementation, rerun the current verification path, and only patch source files if a concrete mismatch is found.

Successful Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`.
- Re-audited `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, and `biofuzz/protein/pocket.py` against the documented phase design.
- Re-verified the workspace with `git branch --show-current`, `python3 -m pytest tests -v`, `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_current_user_state`.
- Confirmed the expected inventory state by checking the documented HIV protease target files and the 10,000-entry seed corpus.
- Updated the required `.agent/` tracking files with a request-specific record of the live audit and validation results.

Outcome:
- Branch status: `develop`.
- Verification status: `41 passed, 10 skipped` and a successful zero-iteration CLI smoke run with `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=10000`, and `best_affinity=0.0`.
- Inventory status: `seeds/zinc_druglike_10k.smi` contains 10,000 entries and the expected HIV protease target/reference files are present.
- Implementation status: the current repository still matches `BIOFUZZ_STRUCTURE.md`, including the documented package layout, target fixtures, scripts, config files, and phase-oriented runtime modules.
- Remaining blocker: `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` are unavailable in this workspace, so chemistry-heavy phases remain environment-blocked rather than code-blocked.
