Task: Review `.agent/AGENTS.md`, validate the current BioFuzz build against `BIOFUZZ_STRUCTURE.md`, and fix any concrete runtime/spec mismatch discovered during the live audit.

Challenges:
- Distinguish between environment-only blockers and real implementation issues.
- Preserve the existing dirty worktree while making a focused fix.
- Keep the CLI preflight behavior consistent with the actual docking runner semantics.

Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md` and audit the live implementation.
- Patch any source-level mismatch found during the audit.
- Add regression coverage for the specific mismatch.
- Re-run the available validation path (`pytest` and CLI smoke/dependency checks).

Completed Approach:
- Re-audited the live BioFuzz implementation and identified a concrete mismatch in `main.py`: the CLI preflight required the configured docking engine to exist even though `biofuzz.docking.runner` already supports falling back to other supported binaries.
- Changed runtime dependency detection to use the docking runner's binary-resolution logic so the CLI and runtime agree on which docking engines are acceptable.
- Added regression coverage for the fallback case in `tests/test_main.py` and re-ran the repository verification path.

Outcome:
- Source change: `main.py` now defers docking-binary availability checks to `biofuzz.docking.runner`'s resolution logic.
- Verification status: `pytest -q` -> `40 passed, 10 skipped`.
- Smoke status: `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_preflight_alignment` completed successfully.
- Remaining blocker: this workspace still lacks `rdkit`, `meeko`, and all supported docking binaries, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
