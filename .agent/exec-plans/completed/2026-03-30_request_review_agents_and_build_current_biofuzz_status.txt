Task: Review `.agent/AGENTS.md` and validate that BioFuzz is built according to `BIOFUZZ_STRUCTURE.md` in the current live workspace.

Challenges:
- Avoid relying on earlier request logs; validate the live tree directly.
- Distinguish missing repository implementation from missing local runtime dependencies.
- Update the required `.agent` tracking artifacts without changing unrelated worktree content.

Approaches considered:
- Rebuild or refactor BioFuzz source modules proactively.
- Audit the current repository against the structure doc, then run the available validation commands and only change source if a real gap appears.

Successful approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`.
- Reconfirm the package layout, target assets, scripts, and key runtime modules match the documented phases.
- Verify the workspace is on `develop`.
- Run `pytest tests -v`.
- Run `python3 .agent/tools/runtime_audit.py`.
- Run `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_final`.
- Record the result in `.agent/PLANS.md`, `.agent/PHASES.md`, and `.agent/USER.md`.

Validation results:
- `git branch --show-current` -> `develop`
- `pytest tests -v` -> `40 passed, 10 skipped`
- `python3 .agent/tools/runtime_audit.py` -> `live_run_ready: False`; `PyYAML` available; `RDKit`, `Meeko`, `gnina`, `vina`, `quickvina2`, and `quickvina-w` unavailable
- `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_current_request_final` -> successful completion with `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`

Outcome:
- The repository is already built to the structure documented in `BIOFUZZ_STRUCTURE.md`.
- No new BioFuzz source-code changes were required for this request.
- The only remaining blocker for a live non-zero docking run in this workspace is the absent chemistry/runtime toolchain.
