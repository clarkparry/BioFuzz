Task: Review `.agent/AGENTS.md`, validate the current BioFuzz workspace against `BIOFUZZ_STRUCTURE.md`, and record the live status for this request.

Challenges:
- Confirm the current repository state directly instead of relying on prior `.agent/` validation history.
- Distinguish between repository completeness and the missing local chemistry/runtime toolchain.
- Update the required project-tracking files without disturbing unrelated work in the existing dirty tree.

Approach:
- Re-read `.agent/AGENTS.md`, `BIOFUZZ_STRUCTURE.md`, and the current tracking files.
- Audit the key runtime modules and repository inventory against the documented phase/module split.
- Run the live verification path for this request: branch check, test suite, runtime audit, and zero-iteration CLI smoke run.
- Record the outcome in `.agent/PLANS.md`, `.agent/PHASES.md`, and `.agent/USER.md`.

Completed Approach:
- Re-read `.agent/AGENTS.md` and confirmed the current workspace should be treated as already built unless a fresh audit found a real gap.
- Re-audited `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/docking/runner.py`, `biofuzz/molecules/preparation.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, and `biofuzz/oracle/selectivity.py` against `BIOFUZZ_STRUCTURE.md`.
- Reconfirmed that the repository still contains the documented package tree, target fixtures, scripts, configuration files, and phase-oriented tests.
- Re-verified the live workspace with `git branch --show-current`, `pytest -q`, `python3 .agent/tools/runtime_audit.py`, and `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_review_agents_and_build_latest`.

Outcome:
- Branch status: `develop`.
- Verification status: `40 passed, 10 skipped`.
- CLI smoke-run outcome: `iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`.
- Runtime audit status: `live_run_ready: False`; `PyYAML` available, `RDKit` and `Meeko` missing, and all supported docking binaries unavailable in this workspace.
- Build status: the repository is already built to the structure spec; the remaining blocker for non-zero docking runs is local environment setup rather than missing BioFuzz project code.
