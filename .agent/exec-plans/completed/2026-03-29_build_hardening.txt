Task: Review AGENTS guidance and harden BioFuzz build stability

Challenges:
- The repository already contained a full scaffold, so changes needed to be surgical and non-disruptive.
- `pytest` and `python3 -m pytest` behaved differently in this environment; direct `pytest` failed import resolution.
- Parser fallback logic for condensed PDBQT lines had a coordinate indexing bug not covered by tests.

Approach:
1. Audited AGENTS requirements and existing implementation against `BIOFUZZ_STRUCTURE.md`.
2. Patched `biofuzz/docking/parser.py` fallback atom parsing to extract coordinates via robust token positions.
3. Added a dedicated regression test in `tests/test_parser.py` for condensed-spacing PDBQT atom lines.
4. Added `tests/conftest.py` and `pytest.ini` so default `pytest` test execution is stable.
5. Re-ran tests with both invocation styles.

Successful completion:
- `python3 -m pytest -q` and `pytest -q` both pass (`9 passed, 4 skipped`).
