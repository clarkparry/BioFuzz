Task: Review .agent/AGENTS.md and validate/build BioFuzz against BIOFUZZ_STRUCTURE.md with a fresh live-tree verification pass.

Challenges:
- Distinguish between a real structure/runtime mismatch and a dependency-limited environment.
- Verify the current tree directly instead of relying on earlier request logs.
- Preserve the dirty worktree while still recording the request in the required planning artifacts.

Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`.
- Audit the live codebase, target inventory, and script inventory against the documented structure.
- Patch any concrete mismatches discovered.
- Run the available verification path: branch check, test suite, zero-iteration CLI smoke run, and dependency audit.

Completed Approach:
- Re-read `.agent/AGENTS.md` and `BIOFUZZ_STRUCTURE.md`, then re-audited the live package structure, key runtime modules, target assets, and script inventory.
- Confirmed the implementation already matches the documented phases and repository layout, so no BioFuzz source-code changes were needed for this request.
- Re-verified the live tree with `git branch --show-current`, `pytest -q`, `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_30_request_build_live`, and a runtime dependency audit.

Outcome:
- Branch status: `develop`.
- Verification status: `39 passed, 10 skipped` and a successful zero-iteration CLI smoke run (`iterations=0`, `hits=0`, `coverage_ratio=0.0`, `corpus_size=6`, `best_affinity=0.0`).
- Inventory status: confirmed the expected `targets/hiv_protease` files and `scripts/` utilities described by the structure document are present.
- Remaining blocker: `rdkit`, `meeko`, and all supported docking binaries are not installed in this workspace, so chemistry-heavy milestones remain environment-blocked rather than code-blocked.
