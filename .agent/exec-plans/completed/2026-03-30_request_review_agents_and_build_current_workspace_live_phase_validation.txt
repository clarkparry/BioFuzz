Task: Review `.agent/AGENTS.md` and validate/build the current BioFuzz workspace against `BIOFUZZ_STRUCTURE.md` using the current live `.venv` runtime.

Challenges:
- Confirm the current workspace directly instead of relying on the long `.agent/` validation history already present in the repository.
- Distinguish between repository-build gaps and runtime-environment gaps, then avoid unnecessary code churn if the implementation is already aligned with the structure document.
- Prove the project is runnable through the real docking path, not only via zero-iteration smoke checks.

Approaches Considered:
- Rebuild or refactor BioFuzz modules preemptively from `BIOFUZZ_STRUCTURE.md` even if the current implementation already matched the document.
- Audit the current code, run the validated `.venv` toolchain, perform direct phase checks plus short live CLI runs, and only change source files if a concrete spec mismatch appears.

Successful Approach:
- Re-read `.agent/AGENTS.md`, `BIOFUZZ_STRUCTURE.md`, and the current repository layout.
- Audited the live implementation modules, including `main.py`, `biofuzz/core/fuzzer.py`, `biofuzz/core/corpus.py`, `biofuzz/core/coverage.py`, `biofuzz/docking/config.py`, `biofuzz/docking/runner.py`, `biofuzz/docking/parser.py`, `biofuzz/molecules/preparation.py`, `biofuzz/molecules/mutator.py`, `biofuzz/oracle/affinity.py`, `biofuzz/oracle/selectivity.py`, `biofuzz/protein/residues.py`, `biofuzz/protein/pocket.py`, and the checked-in HIV protease target assets.
- Verified the workspace is on `develop` and ran `./.venv/bin/python -m pytest tests -q` plus `./.venv/bin/python .agent/tools/runtime_audit.py`.
- Executed a direct phase validation script covering Phase 1 preparation, Phase 2 docking, Phase 3 parsing, Phase 4 coverage, Phase 5 oracle evaluation, Phase 6 corpus ordering, and Phase 7 mutation output checks.
- Executed `./.venv/bin/python main.py --target hiv_protease --max-iterations 1 --workers 1 --output runs/request_2026_03_30_current_turn_live_1w`.
- Executed `./.venv/bin/python main.py --target hiv_protease --max-iterations 4 --workers 2 --output runs/request_2026_03_30_current_turn_live_2w`.
- Recorded the request outcome in `.agent/USER.md`, `.agent/PLANS.md`, and `.agent/PHASES.md`.

Outcome:
- Verification status: `52 passed`.
- Runtime status: `live_run_ready: True` with repo-local `vina` resolved from `.agent/tools/bin/vina`.
- Direct phase-check results: aspirin preparation returned PDBQT with `TORSDOF`; reference indinavir docking succeeded with best affinity `-9.986 kcal/mol`; pose parsing returned `48` atoms; the coverage fingerprint added `11` new pocket-contact bits; the oracle returned a hit; corpus priority popped benzene ahead of ethanol; and `mutate("c1ccc(O)cc1", n=20)` returned `16` valid distinct mutants without echoing the input.
- Live one-worker CLI run completed successfully with `iterations=1`, `hits=0`, `coverage_ratio=0.375`, `corpus_size=10000`, and `best_affinity=-8.746`.
- Live two-worker CLI run completed successfully with `iterations=4`, `hits=0`, `coverage_ratio=0.4166666666666667`, `corpus_size=10003`, and `best_affinity=-8.76`.
- Implementation status: the current repository already satisfies `BIOFUZZ_STRUCTURE.md`; no new BioFuzz source-code changes were necessary for this request.
