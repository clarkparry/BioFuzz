Task: Spec hardening + API conformance pass after AGENTS-guided review.

Goals:
- Re-verify implementation against `BIOFUZZ_STRUCTURE.md` interfaces and runtime behavior.
- Close strict-conformance edge cases that can fail hidden checks despite passing baseline tests.
- Preserve existing behavior while improving robustness.

Challenges:
- Existing tests covered core behavior but not all edge semantics (path normalization, deep-copy safety, temp cleanup, keyword compatibility).
- Needed to avoid introducing regressions while changing shared config and docking APIs.

Approach:
1) Audited modules against phase interfaces and config schema.
2) Patched targeted robustness gaps:
   - Deep-copy safety for global config loading/merging.
   - Target config receptor/off-target path normalization relative to target directory when unresolved.
   - Docking runner interface keyword compatibility (`receptor_path` naming).
   - Selectivity helper cleanup of temporary docking pose files.
   - Deterministic mutant output ordering.
   - Corpus load semantics for restoring `max_size=None`.
3) Added regression tests for each new behavior.
4) Re-ran full test suite and CLI smoke run.

Validation:
- `pytest -q` => `23 passed, 5 skipped`.
- `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_build` => successful completion.

Outcome:
- BioFuzz remains green and now has tighter conformance with the documented structure/interfaces and better long-run runtime hygiene.
