Task: Add a compact regression that protects the real CLI docking path now that BioFuzz has a working `.venv` runtime, repo-local Vina, and checked-in prepared targets.

Challenges:
- Exercise the real `main.py` path instead of another mocked unit path so the regression actually covers argument parsing, target loading, mutation, preparation, docking, checkpointing, and CLI reporting.
- Keep the regression fast and bounded enough for routine `pytest` usage.
- Make the test robust across environments by skipping cleanly when RDKit, Meeko, or a supported docking binary are unavailable.

Approach:
- Add a subprocess-based test in `tests/test_main.py` that launches `main.py` from the repository root against the checked-in `hiv_protease` target.
- Use a tiny single-entry seed file and `--mutations-per-entry 1 --max-iterations 1 --workers 1` so the live run stays bounded.
- Assert the CLI reports one completed iteration and that the expected checkpoint files are produced in the output directory.
- Update the README and `.agent/` tracking files so the new regression is documented as part of the normal verification path.

Completed Approach:
- Added `_live_runtime_available()` and `_parse_cli_stats()` helpers to `tests/test_main.py` plus a new live CLI subprocess regression that runs `main.py` against `hiv_protease` with a one-line phenol seed and auto-skips if RDKit, Meeko, or Vina are unavailable.
- Kept the run bounded to one mutation and one iteration, then asserted the CLI reports `iterations=1`, `corpus_size=2`, `total_docks=1`, `stopped_reason=max_iterations`, a negative `best_affinity`, and the expected `coverage.json`, `corpus/state.json`, and legacy `corpus.json` outputs.
- Updated `README.md`, `.agent/USER.md`, `.agent/PLANS.md`, and `.agent/PHASES.md` to record the decision and the new verification coverage.

Outcome:
- Source change: `pytest` now protects the real one-iteration CLI docking path when the live runtime toolchain is installed.
- Documentation change: the README now calls out that the normal test suite includes a bounded end-to-end docking regression when RDKit, Meeko, and Vina are available.
- Verification status: `./.venv/bin/python -m pytest tests/test_main.py -q` -> `5 passed in 2.14s`; `./.venv/bin/python -m pytest tests -q` -> `60 passed in 2.44s`; `./.venv/bin/python .agent/tools/runtime_audit.py` -> `live_run_ready: True` with repo-local `vina`.
