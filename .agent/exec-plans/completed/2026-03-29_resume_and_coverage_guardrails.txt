Task: Resume + coverage guardrail pass after AGENTS-guided review.

Goals:
- Preserve Phase 8/10 behavior while improving restart semantics for long fuzzing runs.
- Ensure coverage accounting only tracks pocket-defined residues.
- Lock in new behavior with regression tests.

Challenges:
- Keep compatibility with existing tests and CLI behavior.
- Introduce resume logic without forcing seed reloads on resumed runs.

Approach:
1) Audited `core/fuzzer.py` and `core/coverage.py` against the structure document's resume/coverage intent.
2) Implemented targeted changes:
   - Added checkpoint resume loading from `coverage.json` and `corpus.json`.
   - Seed loading now occurs only when no corpus checkpoint is available.
   - Coverage now ignores out-of-pocket residue IDs in updates and load.
3) Added tests:
   - Resume behavior without seed reload.
   - Seed fallback when checkpoints are absent.
   - Coverage pocket filtering regression.
4) Re-ran tests and CLI smoke.

Validation:
- `pytest -q` => `26 passed, 5 skipped`.
- `python3 main.py --target hiv_protease --max-iterations 0 --workers 1 --output runs/smoke_cli_2026_03_29_resume` => successful completion.

Outcome:
- BioFuzz now resumes fuzz campaigns from checkpoints by default and maintains stricter pocket-scoped coverage accounting.
